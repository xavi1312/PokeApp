import { FiltreTipusPipe } from './filtre-tipus.pipe';

describe('FiltreTipusPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreTipusPipe();
    expect(pipe).toBeTruthy();
  });
});
